## コントローラの粒度と名前づけ  
<br>

```
基本の７つ以外のアクションをコントローラに追加していくと、プロジェクトが進むにつれて１つのコントローラにアクションが増えすぎてしまう。
これを避ける１つの指針として「基本の７つのアクション以外のアクションを作成したくなったら別のコントローラを作る」というものがある。
アクションを追加するより、その処理に対応するコントローラを新たに作成する。
コントローラは関連するモデルと同じ名前にするという固定観念から離れて別のリソースとしてコントローラを定義することを考えてみる。
```